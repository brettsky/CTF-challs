
This box demonstrated the exploitation of Log4shell - Enumberation will reveal a vulnerable unifi instance where a remote execution can be achienved by crafing and injecting a JNDI payload into a post req. Then MongoDB can be leveraged to reset the admin password and get into the admin panel. Then we can find plain text SSH creds 


source .venv/bin/activate - starts a python venv 

python3 -m venv /path/to/venv - creates the venv folder



`Script /dev/null -c bash` -upgrades shell 

db.admin.find() - enumerate users within the MongoDB database 

![[Pasted image 20260426133512.png]]

